# ADR 0008: the nested tree is what keeps the zero-dependency promise checkable.
# Under a flat tree a package can import a hoisted development dependency and the
# build still passes.
hoist=false
strict-peer-dependencies=true
